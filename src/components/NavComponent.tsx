import * as React from 'react';
import { render } from "react-dom";

const Nav = () =>{
  return(
    <nav id="page-nav">
      <ul>
        <li>
          <a>Placeholder</a>
        </li>
      </ul>
    </nav>
  );
}

export default Nav;